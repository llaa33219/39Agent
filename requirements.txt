# Core
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
websockets>=10.0
pydantic>=2.0.0
pyyaml>=6.0
pillow>=9.0.0
numpy>=1.24.0
python-multipart>=0.0.6
jinja2>=3.0.0
aiofiles>=23.0.0
httpx>=0.24.0

# AI/ML (datasets>=3.0.0 required for pyarrow 15+ compatibility)
datasets>=3.0.0
sentence-transformers>=2.2.0
transformers>=4.40.0
accelerate>=0.20.0
huggingface-hub>=0.20.0
qwen-vl-utils

# Audio (CosyVoice dependencies are installed separately via scripts/install_cosyvoice.py)
torchaudio>=2.0.0
soundfile>=0.12.0

# Vector DB
lancedb
